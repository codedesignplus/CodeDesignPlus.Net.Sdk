version: '3.8'

services:
  otel-collector:
    image: otel/opentelemetry-collector
    ports:
      - "4317:4317"
      - "4318:4318"
    volumes:
      - ./collector-config.yml:/etc/otelcol/config.yaml
    restart: always
